#include "stdafx.h"
#include <omp.h>
#include <stdlib.h>

int nextMainRow() {
	return 0;
}

void gauss(double** d, int rows, int cols, double* values) {
	for (int i = 0; i < rows; i++) {
		for (int j = 0; j < cols; j++) {

		}
	}
}